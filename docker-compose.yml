version: '3'
services:
  web:
    build: 
      context: condatf2
    ports: 
      - "8888:8888"
    volumes:
      - /app:/usr/app
    command: ["jupyter", "notebook", "--ip=0.0.0.0", "--port=8888", "--allow-root"]

